import React from 'react';

export default function About() {
  return(
    <div style={styles.about}>
      About
    </div>
  )
}

const styles = {
  about: {
    marginTop: 200,
    zIndex: 1,
    height: '100vh'
  }
}
